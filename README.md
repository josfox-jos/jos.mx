# jos.mx — the .jos standard

Home of **.jos**, the deterministic AI engineering standard: compiled, signed,
immutable AI execution contracts. *Buy determinism, not tokens.*

This repo is the deployable landing (static). Built by FOXTANA from the JOS runtime.

- Live: https://jos.mx
- Deploy: `firebase deploy --only hosting`
